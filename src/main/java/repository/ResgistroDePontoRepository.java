package repository;

import model.RegistroDePonto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResgistroDePontoRepository extends JpaRepository<Long, RegistroDePonto> {
}
